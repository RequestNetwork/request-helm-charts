apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "request-node.fullname" . }}-test-connection"
  labels:
{{ include "request-node.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "request-node.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
